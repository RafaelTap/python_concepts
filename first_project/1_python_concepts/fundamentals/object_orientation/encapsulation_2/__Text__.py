#classes attributes and classes methods

"""
classes attributes are located out of __init__ method, check archive "Circle.py"
classes methods are methods inside the classes, used by itself. Are indicated
to access the classes attributes, get access to the memory area where have the
classes attributes, check archive "Circle.py"
@classmethod, means that it can be run directly in classes or in its instances.
__ or _ -> become a method private. check archive MyClass.py.

statics methods: use the property @staticmethod, the method can be called without
reference to object of class. check archive MyClass.py.

"""